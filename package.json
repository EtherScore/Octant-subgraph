{
  "name": "octant-deposits",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "compile": "graph build && graph codegen",
    "deploy subgraph-name": "graph deploy subgraph-name"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.96.0",
    "@graphprotocol/graph-ts": "0.38.0",
    "@types/node": "^22.13.4",
    "csv-parser": "^3.2.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
